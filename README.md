# Multi-RouterNetwork
A virtual networking lab of a small enterprise network with multiple routers and static IP cnfiguration and route configuration.

This lab contains 3 routers which all have a LAN. There is als 2 WAN links and the routes statically configured. There is one end device for each LAN.
